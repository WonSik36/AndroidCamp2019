<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="handong.vacation.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".PapagoActivity">
            <!-- main function -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RecyclerVewActivity">
        </activity>
        <activity android:name=".FileActivity"></activity>
        <activity android:name=".DialogActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".MemoSecondActivity" />
        <activity android:name=".MemoFirstActivity" />
        <activity
            android:name=".FirstActivity"
            android:label="@string/title_activity_first"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".SnackbarActivity" />
        <activity
            android:name=".WebViewActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".RadioActivity" />
        <activity android:name=".helloToast" />
        <activity
            android:name=".Main3Activity"
            android:label="Third Activity" />
        <activity
            android:name=".Main2Activity"
            android:label="Second Activity" />
        <activity
            android:name=".MainActivity"
            android:label="First Activity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>